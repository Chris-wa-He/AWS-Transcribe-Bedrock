# AWS Credentials - 方法1：使用Access Key | Method 1: Using Access Key
AWS_ACCESS_KEY_ID=your_access_key_id
AWS_SECRET_ACCESS_KEY=your_secret_access_key
AWS_REGION=us-east-1

# AWS Credentials - 方法2：使用AWS Profile (推荐) | Method 2: Using AWS Profile (Recommended)
# 如果使用AWS Profile，请注释掉上面的Access Key配置，并取消下面的注释
# If using AWS Profile, comment out the Access Key config above and uncomment below
# AWS_PROFILE=your_aws_profile_name
# AWS_REGION=us-east-1

# S3 Configuration
S3_BUCKET_NAME=your-s3-bucket-name